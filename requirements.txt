# Yaariok Smart System — Python dependencies
# Install: pip install -r requirements.txt
# GPU (RTX 5050 / CUDA 12.8+):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# Web framework
flask

# Environment / config
python-dotenv

# Computer vision
opencv-python
ultralytics

# Speech recognition
SpeechRecognition
pyaudio

# Text-to-speech
edge-tts
pyttsx3
pygame

# HTTP client (LLM API calls)
requests

# Numpy (vision processing)
numpy

# Desktop notifications
plyer
