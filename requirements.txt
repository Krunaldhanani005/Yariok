# Yaariok Smart System — Python dependencies
# Install: pip install -r requirements.txt
# GPU (RTX 5050 / CUDA 12.8+):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# ── Web framework ──────────────────────────────────────────────────────
flask

# ── Environment / config ───────────────────────────────────────────────
python-dotenv

# ── Vision pipeline (Phase B) ──────────────────────────────────────────
opencv-python
ultralytics            # YOLOv11 + BoT-SORT
insightface            # ArcFace `buffalo_l` face embeddings
onnxruntime            # InsightFace inference backend (use onnxruntime-gpu on CUDA hosts)
faiss-cpu              # cosine search over 512-d face embeddings
numpy

# ── MQTT event bus ─────────────────────────────────────────────────────
paho-mqtt              # client used by VisionService + main.py
amqtt                  # pure-Python broker for dev (replace with mosquitto in prod)

# ── Speech / TTS ───────────────────────────────────────────────────────
SpeechRecognition
pyaudio
edge-tts
pyttsx3
pygame

# ── Misc ───────────────────────────────────────────────────────────────
requests               # OpenRouter LLM calls
plyer                  # desktop notifications
